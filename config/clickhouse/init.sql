-- ClickHouse initialization script for TensorZero
-- Create the tensorzero database if it doesn't exist
-- TensorZero will create its own tables automatically

CREATE DATABASE IF NOT EXISTS tensorzero;